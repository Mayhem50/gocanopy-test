{
  "name": "@gocanopy/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "gocanopy": "./dist/index.js"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest --run --passWithNoTests",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@gocanopy/core": "workspace:*"
  }
}
