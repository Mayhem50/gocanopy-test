{
  "name": "@gocanopy/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest --run",
    "build": "tsc -p tsconfig.build.json",
    "serve": "tsx src/index.ts"
  },
  "dependencies": {
    "@gocanopy/bank": "workspace:*",
    "@gocanopy/projection": "workspace:*",
    "@gocanopy/scan": "workspace:*",
    "@hono/node-server": "catalog:",
    "hono": "catalog:",
    "tsx": "catalog:"
  }
}
